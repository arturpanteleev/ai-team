Delivery больше не исполняется LLM-агентом.

Контроллер детерминированно строит `delivery-plan.json` только из файлов,
изменённых попытками текущего run, требует отдельное подтверждение, добавляет в
index только точные пути из плана, запрещает push в protected branch и сохраняет
результат каждого commit/push/PR шага для безопасного resume.
