## ДОБАВЛЕННЫЕ Требования

### Требование: Конфиг по умолчанию
Система ДОЛЖНА создавать `.ai-team/config.yaml` с разумными значениями по умолчанию.

#### Сценарий: Структура конфига
- **КОГДА** `ai-team init` запускается
- **ТОГДА** config.yaml ДОЛЖЕН содержать:
  - `pipeline: <list of agents>`
  - `cli: opencode`
  - `model: auto`
  - `effort: medium`

### Требование: Кастомный путь конфига
Система ДОЛЖНА поддерживать флаг `--target` для указания директории целевого проекта.

#### Сценарий: Init в кастомной директории
- **КОГДА** пользователь запускает `ai-team init --target /path/to/project`
- **ТОГДА** система ДОЛЖНА создать `/path/to/project/.ai-team/` вместо `./.ai-team/`

### Требование: Gitignore
Система ДОЛЖНА добавлять `.ai-team/` в `.gitignore`, если его там нет.

#### Сценарий: Авто-добавление в gitignore
- **КОГДА** `ai-team init` запускается и `.gitignore` существует
- **ТОГДА** система ДОЛЖНА дописать `.ai-team/` в `.gitignore`, если его там ещё нет

### Требование: Директория reports при инициализации
Система ДОЛЖНА создавать `.ai-team/reports/` при `ai-team init`.

#### Сценарий: Init создаёт reports
- **КОГДА** `ai-team init` запускается
- **ТОГДА** система ДОЛЖНА создать `.ai-team/reports/` директорию
- **И** `.ai-team/reports/` ДОЛЖНА быть добавлена в `.gitignore`

### Требование: Обновлённый конфиг по умолчанию
Конфиг по умолчанию ДОЛЖЕН включать `effort`.

#### Сценарий: Структура конфига
- **КОГДА** `ai-team init` запускается
- **ТОГДА** config.yaml ДОЛЖЕН содержать:
  - `pipeline:` с именами агентов
  - `cli: opencode`
  - `model: auto`
  - `effort: medium`
